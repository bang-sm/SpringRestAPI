<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.toggle.study.mapper.MyPageMapper">



	<select id="custqustinfo"  resultType="com.toggle.study.entity.CustQust" parameterType="string">
        SELECT
			*
		FROM
			cust_qust
		WHERE
			qust_no=#{qustNo}
	</select>

</mapper>